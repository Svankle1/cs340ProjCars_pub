{{!-- Create a table --}}
<table id="people-table" border="1" cellpadding="5">>
    <script src="http://ajax.googleapis.com/ajax/libs/jquery/1.9.1/jquery.min.js"></script>
    {{!-- Header section --}}
    <thead>

        {{!-- For just the first row, we print each key of the row object as a header cell so we
        know what each column means when the page renders --}}
        <tr>
            {{#each data.[0]}}
            <th>
                {{@key}}
            </th>
            {{/each}}
        </tr>
    </thead>

    {{!-- Body section --}}
    <tbody>

        {{!-- For each row, print the id, fname, lname, homeworld and age, in order --}}
        {{#each data}}
        <tr data-value={{this.personID}}>>
            <td>{{this.name}}</td>
            <td>{{this.personID}}</td>
            <td>{{this.networth}}</td>
            <td>{{this.total_cars}}</td>
            <td><button onclick="deletePerson({{this.personID}})">Delete</button></td>
        </tr>
        {{/each}}
    </tbody>
</table>
<script src="/js/delete_person.js"></script>
<h2>Adding Data using an HTML form</h2>
        <p>To add a new person, please enter their information below nad click `Submit`!</p>
        <form id="add-person-form" method="POST" action="add-person-form">
            <label for="input-name">Name: </label>
            <input type="text" name="input-name" id="input-name">

            <label for="input-networth">Net Worth: </label>
            <input type="text" name="input-networth" id="input-networth">
            
            <br>
            {{#each cars}}
            <input type="checkbox" id="vehicle{{this.autoID}}" name="vehicle{{this.autoID}}" value="{{this.autoID}}">
            <label for="vehicle{{this.autoID}}">{{this.name}}</label><br>
            {{/each}}
            <input type="submit">
        </form>

<h2>Updating A Person's Collection</h2>
<p>To update a specific persons's collection (&amp stats) select their name from the drop-down menu on the left and their desired homeworld from the drop down menu on the right</p>
<form id="update-person-form-ajax">

    <label for="input-fullname">Full name: </label>
    <select name="input-fullname" id="mySelect" onchange="updateValues(document.getElementById('mySelect').value)">
        <option value="0">Select a Person</option>
        {{#each data}}
        <option value="{{this.personID}}">{{this.name}}</option>
        {{/each}}
    </select>

    <label for="input-networth">Net Worth: </label>
    <input type="text" name="input-networth-update" id="input-networth-update">


    <br>
    {{#each cars}}
    <input type="checkbox" tag="updateCheck" id="u_vehicle{{this.autoID}}" name="u_vehicle{{this.autoID}}" value="{{this.autoID}}">
    <label for="u_vehicle{{this.autoID}}">{{this.name}}</label><br>
    {{/each}}

    <input type="submit">

</form>

<script src="/js/update_person.js"></script>